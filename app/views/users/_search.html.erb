 <%= form_with(method: :get) do |form| %>
      <%= form.label :query, "Search by name:" %>
      <%= form.text_field :query, data: { action: "input->user-search#search" }, autocomplete: "off", value: params[:query] %>
    <% end %>